_G.S = _G.S or {}
_G.S.infiniteRebirthActive = true

local RS = game:GetService("ReplicatedStorage")

spawn(function ()
    while _G.S.infiniteRebirthActive do
        wait(0.1)

        pcall(function()
            RS.rEvents.rebirthRemote:InvokeServer("rebirthRequest")
        end)
    end
end)
